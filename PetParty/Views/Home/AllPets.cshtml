@model List<Pet>

<h1>All The Pets in the Party</h1>
@foreach (Pet p in Model)
{
    <p>@p.Name is a @p.Age year old @p.Species they @(p.IsTheGoodest ? "are" : "aren't") the goodest</p>
}
<a class="btn btn-primary" asp-action="Index" asp-controller="Home">Form</a>